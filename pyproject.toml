[project]
name = "email-waterfall"
version = "1.3.2"
description = "Standalone MCP service: DM / work-email / cellphone enrichment waterfall"
requires-python = ">=3.11"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
